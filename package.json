{
  "name": "airtable-migration-audit",
  "version": "1.0.0",
  "description": "Audit Airtable bases for migration readiness — schema analysis, data quality checks, complexity scoring, and actionable migration plan",
  "type": "module",
  "scripts": {
    "discover": "tsx src/discover.ts",
    "discover:schema": "tsx src/discover.ts --schema-only"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^25.3.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
